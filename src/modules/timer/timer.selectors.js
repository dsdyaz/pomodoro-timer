export const rootSelector = (state) => state

export const SelectRemainingTime = (state) => state.timer.remainingTime

export const SelectRunning = (state) => state.timer.isRunning

export const SelectRest = (state) => state.timer.rest
